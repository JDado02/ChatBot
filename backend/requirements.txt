# --- Runtime ---
psycopg[binary]>=3.1      # driver PostgreSQL (sync ora; async al Passo 6 con FastAPI)
httpx>=0.27               # client HTTP verso Ollama (embedding)

# --- Dev / test ---
pytest>=8.0

# Nota: gli embedding sono formattati a mano come literal pgvector
# (app/vectors.to_pgvector), quindi il pacchetto 'pgvector' non è richiesto.
# Modello di embedding in dev: Ollama (es. `ollama pull bge-m3`), non pip.
